// 1) We can group tasks by type in SQL not in JPQL
// 2) We can update delete and do ddl operations with SQL but cannot do with JPQL
// 3) Perform join with table that is not   mapped as entity
// 4) Rank or number tasks by dueDate or hourlyRate(if they're billable)
